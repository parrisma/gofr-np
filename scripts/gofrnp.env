#!/bin/bash
# Compatibility shim: the canonical env preset is now scripts/project.env.
# NOTE: no set -e here — this file is designed to be sourced, not executed.

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# shellcheck source=/dev/null
source "$SCRIPT_DIR/project.env"
